#include "common.xrh"

struct v_in
{
    float4 P : POSITION;
};

struct v_out
{
    float4 hpos : POSITION;
};

v_out main(v_in I)
{
    v_out O;

    O.hpos = mul(m_WorldViewProject, I.P);

    return O;
}
