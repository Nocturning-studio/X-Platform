function normal(shader, t_base, t_second, t_detail)
	shader:begin("screen_quad","main_menu_gamma")
		: fog(false)
		: zb (false,false)
	shader:sampler("s_base"):texture("$user$generic1"): clamp()
end
